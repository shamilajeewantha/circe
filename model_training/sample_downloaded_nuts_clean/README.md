# model_training/sample_downloaded_nuts_clean/

Cleaned/curated subset of `sample_downloaded_nuts/` (renamed `nut_0001.jpg`, ...) used
as a tidy `SOURCE` for `predict.py` smoke tests.

**Contents are gitignored** — everything except this README is excluded. The folder is
kept so the path resolves. Regenerate by re-cleaning `sample_downloaded_nuts/`.
